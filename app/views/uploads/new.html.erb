<div class="max-w-2xl mx-auto px-6 py-12">
  <div class="mb-10">
    <h1 class="text-4xl font-bold text-gray-900 mb-2">Upload CSV</h1>
    <p class="text-gray-600">Import and analyze your data</p>
  </div>

  <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
    <%= form_with model: @upload, local: true, multipart: true do |f| %>
      <% if @upload.errors.any? %>
        <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-xl">
          <h3 class="text-red-900 font-semibold mb-2 flex items-center space-x-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span>Please fix the following errors:</span>
          </h3>
          <ul class="list-disc list-inside text-red-700 text-sm space-y-1">
            <% @upload.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="mb-8">
        <%= f.label :name, "Upload Name", class: "block text-sm font-semibold text-gray-900 mb-3" %>
        <%= f.text_field :name, class: "w-full px-4 py-3 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all text-gray-900 placeholder-gray-400", placeholder: "e.g., Sales Data Q4 2024" %>
        <p class="mt-2 text-xs text-gray-500">Optional - leave blank to auto-generate</p>
      </div>

      <div class="mb-8">
        <%= f.label :industry_type, "Industry Type", class: "block text-sm font-semibold text-gray-900 mb-3" %>
        <%= f.select :industry_type, options_for_select(IndustryPromptsService.all_industries, @upload.industry_type || "other"), {}, { class: "w-full px-4 py-3 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all text-gray-900 bg-white" } %>
        <p class="mt-2 text-xs text-gray-500">Select your industry to get customized Power BI analysis prompts</p>
      </div>

      <div class="mb-8">
        <%= f.label :file, "CSV File", class: "block text-sm font-semibold text-gray-900 mb-3" %>
        <div class="relative">
          <div class="flex items-center justify-center w-full">
            <label class="flex flex-col items-center justify-center w-full h-40 border-2 border-dashed border-gray-300 rounded-2xl cursor-pointer hover:border-purple-400 hover:bg-purple-50/50 transition-all group">
              <div class="flex flex-col items-center justify-center pt-5 pb-6">
                <svg class="w-12 h-12 mb-4 text-gray-400 group-hover:text-purple-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
                </svg>
                <p class="mb-2 text-sm text-gray-500 group-hover:text-purple-600 transition-colors">
                  <span class="font-semibold">Click to upload</span> or drag and drop
                </p>
                <p class="text-xs text-gray-400">CSV files only</p>
              </div>
              <%= f.file_field :file, accept: ".csv", required: true, class: "hidden", id: "csv-file-input", onchange: "document.getElementById('file-name').textContent = this.files[0]?.name || 'No file chosen'" %>
            </label>
          </div>
        </div>
        <p id="file-name" class="mt-3 text-sm text-gray-600 font-medium"></p>
        <p class="mt-2 text-xs text-gray-500">
          The CSV file should have headers in the first row. Column types are detected automatically.
        </p>
      </div>

      <div class="flex items-center justify-between pt-6 border-t border-gray-100">
        <%= link_to uploads_path, class: "text-gray-600 hover:text-gray-900 font-medium text-sm transition-colors" do %>
          Cancel
        <% end %>
        <%= f.submit "Upload CSV", class: "bg-gradient-to-r from-purple-600 to-blue-600 text-white px-8 py-3 rounded-full font-semibold hover:shadow-xl transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-offset-2" %>
      </div>
    <% end %>
  </div>

  <div class="mt-8 bg-gradient-to-br from-purple-50 to-blue-50 rounded-2xl border border-purple-100 p-6">
    <h3 class="text-sm font-semibold text-gray-900 mb-4 flex items-center space-x-2">
      <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <span>How it works</span>
    </h3>
    <ul class="grid grid-cols-1 md:grid-cols-2 gap-3 text-sm text-gray-700">
      <li class="flex items-start space-x-3">
        <svg class="w-5 h-5 text-purple-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        <span>Automatic schema detection for any CSV structure</span>
      </li>
      <li class="flex items-start space-x-3">
        <svg class="w-5 h-5 text-purple-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        <span>Data types inferred intelligently</span>
      </li>
      <li class="flex items-start space-x-3">
        <svg class="w-5 h-5 text-purple-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        <span>Stored securely in PostgreSQL</span>
      </li>
      <li class="flex items-start space-x-3">
        <svg class="w-5 h-5 text-purple-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        <span>Optional Power BI push for AI analysis</span>
      </li>
    </ul>
  </div>
</div>